import { ColoredborderDirective } from './coloredborder.directive';

describe('ColoredborderDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredborderDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
